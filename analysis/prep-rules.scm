(define (prep-rule-0)
  (ground "$be" "be")
  (set-link-type "$prep" "preposition")
  (if (match-rule? '((_obj $be $var2) ($prep $var1 $var2)))
      (rule-applied "prep-rule-0")
      )
  (reset-scope)
  )
(define (prep-rule-1)
  (ground "$be" "be")
  (set-link-type "$prep" "preposition")
  (if (match-rule? '((_subj $be $var0) ($prep $var1 $var2)))
      (rule-applied "prep-rule-1")
      )
  (reset-scope)
  )
(define (prep-rule-2)
  (set-link-type "$prep" "preposition")
  (if (match-rule? '((_predadj $var1 $var0) ($prep $var1 $var2)))
      (rule-applied "prep-rule-2")
      )
  (reset-scope)
  )
(define (prep-rule-3)
  (set-link-type "$prep" "preposition")
  (if (match-rule? '((_obj $var1 $var0) ($prep $var1 $var2)))
      (rule-applied "prep-rule-3")
      )
  (reset-scope)
  )
(define (prep-rule-4)
  (set-link-type "$prep" "preposition")
  (if (match-rule? '((_subj $var1 $var0) ($prep $var1 $var2)))
      (rule-applied "prep-rule-4")
      )
  (reset-scope)
  )

(define (run-rules)
  (prep-rule-0)
  (prep-rule-1)  
  (prep-rule-2)
  (prep-rule-3)
  (prep-rule-4)
)
(run-rules)
